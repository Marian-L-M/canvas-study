const canvas = document.getElementById("hello-world-canvas");
const context = canvas.getContext("2d")


// Opsie! sorry future Marian I deleted that section by accident. Have fun digging it up on Git.
// Basically there is a function to get a canvas url (default png which you can pass to an img tag)